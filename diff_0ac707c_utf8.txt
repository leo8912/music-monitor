diff --git a/check_api_debug.py b/check_api_debug.py
new file mode 100644
index 0000000..68dfc15
--- /dev/null
+++ b/check_api_debug.py
@@ -0,0 +1,37 @@
+import asyncio
+import json
+from sqlalchemy import select, func
+from sqlalchemy.orm import selectinload
+from core.database import AsyncSessionLocal
+from app.models.artist import Artist
+from app.models.song import Song
+
+async def debug_api_response():
+    async with AsyncSessionLocal() as db:
+        # 妯℃嫙 SubscriptionService.get_monitored_artists 鐨勯€昏緫
+        stmt = (
+            select(Artist, func.count(Song.id).label('song_count'))
+            .options(selectinload(Artist.sources))
+            .outerjoin(Song, Song.artist_id == Artist.id)
+            .where(Artist.is_monitored == True)
+            .group_by(Artist.id)
+            .order_by(Artist.id.asc())
+        )
+        result = await db.execute(stmt)
+        artists_with_counts = result.unique().all()
+        
+        res = [
+            {
+                "name": a.name,
+                "id": str(a.id),
+                "source": "database",
+                "sources": [s.source for s in a.sources] if a.sources else [],
+                "avatar": a.avatar,
+                "songCount": song_count or 0
+            }
+            for a, song_count in artists_with_counts
+        ]
+        print(json.dumps(res, indent=2, ensure_ascii=False))
+
+if __name__ == "__main__":
+    asyncio.run(debug_api_response())
diff --git a/check_artist_count.py b/check_artist_count.py
new file mode 100644
index 0000000..dca5ea0
--- /dev/null
+++ b/check_artist_count.py
@@ -0,0 +1,33 @@
+import asyncio
+from sqlalchemy import select, func
+from sqlalchemy.orm import selectinload
+from core.database import AsyncSessionLocal
+from app.models.artist import Artist
+from app.models.song import Song
+
+async def test_artist_count():
+    async with AsyncSessionLocal() as db:
+        # 妫€鏌ユ墍鏈夎壓浜虹姸鎬?+        stmt_all = select(Artist)
+        res_all = await db.execute(stmt_all)
+        all_artists = res_all.scalars().all()
+        print(f"\nAll artists in DB: {len(all_artists)}")
+        for a in all_artists:
+            print(f"Name: {a.name}, ID: {a.id}, Monitored: {a.is_monitored}")
+
+        stmt = (
+            select(Artist, func.count(Song.id).label('song_count'))
+            .options(selectinload(Artist.sources))
+            .outerjoin(Song, Song.artist_id == Artist.id)
+            .where(Artist.is_monitored == True)
+            .group_by(Artist.id)
+            .order_by(Artist.id.asc())
+        )
+        result = await db.execute(stmt)
+        unique_rows = result.unique().all()
+        print(f"\nMonitored artists with counts: {len(unique_rows)}")
+        for a, count in unique_rows:
+            print(f"Artist: {a.name}, ID: {a.id}, Count: {count}")
+
+if __name__ == "__main__":
+    asyncio.run(test_artist_count())
diff --git a/check_song_status.py b/check_song_status.py
new file mode 100644
index 0000000..9ac97f4
--- /dev/null
+++ b/check_song_status.py
@@ -0,0 +1,18 @@
+import asyncio
+from sqlalchemy import select, func
+from sqlalchemy.orm import selectinload
+from core.database import AsyncSessionLocal
+from app.models.artist import Artist
+from app.models.song import Song
+
+async def test_song_status():
+    async with AsyncSessionLocal() as db:
+        # 缁熻瀹跨窘闃?ID: 4)鐨勬瓕鏇茬姸鎬?+        stmt = select(Song.status, func.count(Song.id)).where(Song.artist_id == 4).group_by(Song.status)
+        result = await db.execute(stmt)
+        print(f"\nSong status distribution for Artist ID 4 (瀹跨窘闃?:")
+        for status, count in result.all():
+            print(f"Status: {status}, Count: {count}")
+
+if __name__ == "__main__":
+    asyncio.run(test_song_status())
diff --git a/debug_output.txt b/debug_output.txt
index bc451d0..ce9abb8 100644
--- a/debug_output.txt
+++ b/debug_output.txt
@@ -5,9 +5,14 @@
 姝屾洸: 鎴戣浣?(ID: 1)
   Source ID: 1, Source: local
   data_json 绫诲瀷: str
-  鍖呭惈姝岃瘝: False
-  姝岃瘝棰勮: N/A...
-  鍏朵粬瀛楁: ['quality', 'format', 'cover']
+  鍖呭惈姝岃瘝: True
+  姝岃瘝棰勮: [00:00.00] 浣滆瘝 : 妯婂啿
+[00:00.00] 浣滄洸 : 妯婂啿
+[00:00.00]鎴戣浣?- 浠荤礌姹?+[00:00.57]璇嶏細妯婂啿
+[00:01.14]鏇诧細妯婂啿
+[00:01.71]鎴戣...
+  鍏朵粬瀛楁: ['quality', 'format', 'cover', 'lyrics']
 
 姝屾洸: 涓€鐢熸墍鐖?(ID: 2)
   Source ID: 2, Source: local
diff --git a/web/src/stores/library.ts b/web/src/stores/library.ts
index 1e6d416..bdb4829 100644
--- a/web/src/stores/library.ts
+++ b/web/src/stores/library.ts
@@ -39,7 +39,7 @@ export const useLibraryStore = defineStore('library', () => {
                     avatar: a.avatar,
                     sources: sourceList,
                     ids: [idEntry],
-                    song_count: a.song_count || 0
+                    songCount: a.songCount || 0
                 })
             }
             // Since backend is now normalized, duplicates by name shouldn't happen for Monitored Artists.
diff --git a/web/src/types/artist.ts b/web/src/types/artist.ts
index 0fce945..36ca1a1 100644
--- a/web/src/types/artist.ts
+++ b/web/src/types/artist.ts
@@ -11,7 +11,7 @@ export interface Artist {
     source: MusicSource // "database"
     sources?: MusicSource[] // Linked sources
     avatar?: string
-    song_count?: number
+    songCount?: number
     available_sources?: string[]
     is_monitored?: boolean
 }
@@ -22,7 +22,7 @@ export interface MergedArtist {
     avatar?: string
     sources: MusicSource[]
     ids: Array<{ source: MusicSource; id: string }>
-    song_count?: number
+    songCount?: number
 }
 
 // 鎼滅储缁撴灉涓殑姝屾墜
