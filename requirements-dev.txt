# ============================================================================
# 开发 / 测试依赖 (Development & Test Dependencies)
# ============================================================================
#
# 用途：本文件只包含"跑测试 + 静态检查"所需的依赖，不包含运行时依赖。
#
# 本地开发：
#     pip install -r requirements.txt -r requirements-dev.txt
#     pytest -q
#
# CI（.github/workflows/test.yml）中同样通过上面这条命令安装。
#
# 版本策略：只使用下界约束（>=），不锁死到某个具体小版本，避免出现
# "锁到不存在的版本导致 CI 直接装不上" 的问题。若将来需要可复现构建，
# 再引入 pip-compile / uv lock 生成 requirements-dev.lock。
# ============================================================================

# --- 测试框架 ---
pytest>=8.0
pytest-asyncio>=0.23        # 配合 pytest.ini 的 asyncio_mode=auto 使用
pytest-cov>=5.0             # CI 覆盖率门禁 (--cov-fail-under) 依赖此插件
pytest-mock>=3.12           # mocker fixture，替代裸 unittest.mock.patch 嵌套

# --- HTTP / 异步接口测试 ---
httpx>=0.27                 # ASGITransport 直连 FastAPI，无需起真实端口
respx>=0.20                 # mock httpx 出站请求（各音乐平台 Provider）
aioresponses>=0.7           # mock aiohttp 出站请求（部分 Provider 用的是 aiohttp）

# --- 测试工具 ---
freezegun>=1.5              # 冻结时间，测试"新歌发布时间/定时任务"等时间相关逻辑

# --- 静态检查 ---
ruff>=0.6                   # lint + format 检查，CI 里做 F821 等致命错误扫描
