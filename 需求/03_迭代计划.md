# 迭代计划与里程碑

> 版本: 1.0.0
> 描述: 记录 Music Monitor 项目的演进阶段。

## 阶段一：基础架构重构 (已完成 - 2026-02)
- 提取 Service/Repository 模式。
- 单例模式应用（MusicAggregator/DownloadService）。
- 初步的数据和 API 标准化（snake_case 接口、camelCase 前端）。

## 阶段二：系统韧性增强 (已完成)
- 将单次网络波动的敏感任务修改为带冷却或递增重试。
- 清理前端状态显示 Bug，完成 `song_count` 参数重命名全映射。
- 重构全量系统文档，消灭悬空碎片文档。

## 阶段三：智能与扩容 (规划中)
- 分词搜索、智能纠错推荐。
- 定制化推送内容排版强化。
- Websocket 等异步长连接支持。

## 阶段四：API 层标准化 (已完成 - 2026-08-13)
- `app/routers/` 重排为 `app/api/v1/`（URL 全部不变，前端 96 处硬编码 /api/ 路径零改动）。
- 拆分 system.py（206 行 → status/logs/scan/notify/misc 5 文件）与 media.py（306 行 → download/player/history/mobile 4 文件）。
- 全部端点补 `response_model`（schemas.py 新增 ~19 个响应模型）；分页统一 page/page_size + PaginatedResponse。
- 认证改为路由级 `dependencies=[Depends(require_auth)]`；删除冗余 `app/routers/download.py`、EventBus（零订阅者）、重复 /ws/progress。
- 修 `from notifiers.telegram import` 路径错误。

## 阶段五：前端重构 (已完成 - 2026-08-13)
- 删除孤儿组件（SettingsModal/SettingsModalV2/ProfileModal/RepairModal/TaskCenter 等）。
- `views/` 合并进 `desktop/`（LyricsView / MobilePlay 归位）。
- stores 统一导出（6 个 store 经 index.ts 汇总）；组件散落 axios 调用收敛到 api/ 模块。
- 全页面 walkthrough 验收：登录/Home/搜索/添加艺人/Settings/Library/History/ArtistDetail/Lyrics/播放链路/MobilePlay 端到端。
- 修复 MobilePlay 签名链路 500（3 层根因：id 格式、song 属性访问、sources 关系未加载）。

## 阶段六：部署收尾 (已完成 - 2026-08-13)
- Dockerfile：supervisord 三进程（redis-server/api/worker）+ 多阶段瘦身 + HEALTHCHECK。
- docker-compose：healthcheck + SQLite WAL 伴随文件同卷 + 内置 Redis AOF 持久化。
- CI：新增 frontend job（vue-tsc 阻断 + build 阻断）。
- song_sources 去重迁移（a3f9c2e1d4b5：删 4 条重复 + 补唯一约束）+ `upsert_source` 幂等防护。
- MobilePlay.vue 加 watch route.query（同路径参数切换刷新）。
- 更新 后端服务架构说明.md v3.0 与 README.md 部署章节。
